server.port=0

spring.application.name=initial-service
eureka.instance.hostname=localhost

eureka.client.serviceUrl.defaultZone=http://localhost:8010/eureka/
eureka.instance.instance-id=${spring.application.name}